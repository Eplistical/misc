#ifndef _TYPES_HPP
#define _TYPES_HPP
// types macro

#include <cstdint>
#include <string>
#include <complex>

using VOID_T = void;
using CHAR_T = char;
using BOOL_T = bool;
using INT_T = int32_t;
using UINT_T = uint32_t;
using INT32_T = int32_t;
using UINT32_T = uint32_t;
using INT64_T = uint64_t;
using UINT64_T = uint64_t;
using UNSIGNED_T = unsigned;
using LONG_T = long;
using LONG_LONG_T = long long;
using UNSIGNED_LONG_T = unsigned long;
using UNSIGNED_LONG_LONG_T = unsigned long long;
using SIZE_T = size_t;
using FLOAT_T = float;
using DOUBLE_T = double;
using LONG_DOUBLE_T = long double;
using STRING_T = std::string;
using COMPLEX_FLOAT_T = std::complex<float>;
using COMPLEX_DOUBLE_T = std::complex<double>;
using COMPLEX_T = std::complex<double>;

#endif
